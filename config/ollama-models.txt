# AI Workstation — Ollama modellen
# Ollama draait in WSL, niet in Windows (geen Ollama.Ollama in winget).
# Volgorde: primair eerst.

# Primair — qwen3-14b-atlas (Qwen3 14B, 128K ctx, tool-support)
# Gebouwd via: ollama create qwen3-14b-atlas -f ~/.hermes/modelfiles/qwen3-14b-atlas.Modelfile
qwen3:14b

# Fallback — qwen3-8b-atlas (Qwen3 8B, 128K ctx, tool-support)
# Gebouwd via: ollama create qwen3-8b-atlas -f ~/.hermes/modelfiles/qwen3-8b-atlas.Modelfile
qwen3:8b

# Vision / afbeeldingsanalyse
gemma4:e4b

# Honcho embeddings (nomic-embed-text, 274 MB, 768-dim)
nomic-embed-text
